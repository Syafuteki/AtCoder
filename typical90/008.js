"use strict";

function Main(input) {
    //入力を整理
    input = input.trim().split(/\n/).map(String);
    const N = Number(input.shift());
    const S = input.shift();
    const num = 10 ** 9 + 7;

}

Main(require('fs').readFileSync('/dev/stdin', 'utf8'));